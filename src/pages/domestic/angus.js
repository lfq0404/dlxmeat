import * as React from "react";
import Layout from "../../components/layout";

const AngusPage = () => {
    return (
        <Layout>
            <div>import</div>
        </Layout>
    );
};

export default AngusPage;
