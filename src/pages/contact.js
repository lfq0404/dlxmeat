import * as React from "react";
import Layout from "../components/layout";

const ContactPage = () => {
  return (
    <Layout>
      <div>contact</div>
    </Layout>
  );
};

export default ContactPage;
